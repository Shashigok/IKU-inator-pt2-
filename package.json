{
  "name": "iframe-proxy-server",
  "version": "1.0.0",
  "description": "A Node.js proxy that strips X-Frame-Options and CSP headers so any URL can be embedded in an iframe.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
